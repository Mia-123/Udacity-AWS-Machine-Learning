from setuptools import setup

setup(name='Binomial_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions'],
      author='Mia_Z',
      zip_safe=False)
